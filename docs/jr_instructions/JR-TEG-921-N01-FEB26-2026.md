# [TEG] [TEG] Basin Signal Wiring + Pool Fix - Node n01

**Parent Task**: #921
**Auto-decomposed by TEG Planner**: 2026-02-26T08:12:58.402136
**Operation**: search_replace
**Target File**: `/ganuda/daemons/tpm_autonomic_v2.py`
**Dependencies**: n00

---

File: `/ganuda/daemons/tpm_autonomic_v2.py`

<<<<<<< SEARCH
def claim_task(task_id):
    """Atomically claim a task (pending → in_progress) with row locking."""
    conn = get_db()
    cur = conn.cursor()
    cur.execute("""
        UPDATE jr_work_queue
        SET status = 'in_progress', started_at = NOW(), updated_at = NOW(),
            status_message = 'Claimed by TPM Autonomic Daemon v2'
        WHERE id = %s AND status = 'pending'
        RETURNING id
    """, (task_id,))
    claimed = cur.fetchone()
    conn.commit()
    cur.close()
    conn.close()
    return claimed is not None
=======
def claim_task(task_id):
    """Atomically claim a task (pending → in_progress) with row locking."""
    conn = get_db()
    try:
        cur = conn.cursor()
        cur.execute("""
            UPDATE jr_work_queue
            SET status = 'in_progress', started_at = NOW(), updated_at = NOW(),
                status_message = 'Claimed by TPM Autonomic Daemon v2'
            WHERE id = %s AND status = 'pending'
            RETURNING id
        """, (task_id,))
        claimed = cur.fetchone()
        conn.commit()
        cur.close()
        return claimed is not None
    finally:
        put_db(conn)
>>>>>>> REPLACE
