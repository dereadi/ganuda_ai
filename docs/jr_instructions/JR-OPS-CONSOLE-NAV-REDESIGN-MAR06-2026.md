# Jr Instruction: Ops Console Navigation Redesign

**Task**: Add consistent navigation header to all ganuda.us pages
**Priority**: 5
**Story Points**: 3
**Epic**: #1974

## Context

Each page on ganuda.us (status, health, briefing, blog) is standalone with no way to navigate between them. We need a shared nav snippet. Since these are static HTML pages generated by Python scripts, each generator needs to include the same nav HTML.

Create a shared Python module that returns the nav HTML, then wire it into existing generators.

## Steps

### Step 1: Create shared nav module

Create `/ganuda/scripts/web_nav.py`

```python
"""Shared navigation HTML for ganuda.us pages."""


def nav_html(active_page=""):
    """Return nav bar HTML. active_page should match one of the link names."""
    links = [
        ("Home", "/index.html"),
        ("Status", "/status.html"),
        ("Health", "/health.html"),
        ("Briefing", "/briefing.html"),
        ("Blog", "/blog/index.html"),
    ]
    items = ""
    for name, href in links:
        cls = ' class="active"' if name.lower() == active_page.lower() else ""
        items += f'<a href="{href}"{cls}>{name}</a> '

    return f"""<nav style="margin-bottom:16px; padding:8px 0; border-bottom:1px solid #1a2030;">
  <span style="color:#e8b04a; font-weight:600; margin-right:12px;">Cherokee AI</span>
  {items}
</nav>
<style>
  nav a {{ color:#556; text-decoration:none; margin-right:10px; font-size:0.85em; }}
  nav a:hover {{ color:#7aafff; }}
  nav a.active {{ color:#7aafff; font-weight:600; }}
</style>"""
```

### Step 2: Wire nav into status page generator

File: `/ganuda/scripts/generate_status_page.py`

```
<<<<<<< SEARCH
<body>
<h1>&#127793; Cherokee AI Federation</h1>
=======
<body>
{{nav}}
<h1>&#127793; Cherokee AI Federation</h1>
>>>>>>> REPLACE
```

And add the nav import and replacement at the top of render_html:

File: `/ganuda/scripts/generate_status_page.py`

```
<<<<<<< SEARCH
def render_html(stats):
    now = datetime.now().strftime("%Y-%m-%d %H:%M CT")
=======
def render_html(stats):
    from scripts.web_nav import nav_html
    nav = nav_html("Status")
    now = datetime.now().strftime("%Y-%m-%d %H:%M CT")
>>>>>>> REPLACE
```

Then at the end of render_html, before returning:

File: `/ganuda/scripts/generate_status_page.py`

```
<<<<<<< SEARCH
    return html
=======
    html = html.replace("{nav}", nav)
    return html
>>>>>>> REPLACE
```

## Verification

1. Import check: `python3 -c "from scripts.web_nav import nav_html; print(nav_html('Status')[:50])"`
2. Run status page generator: `cd /ganuda && python3 scripts/generate_status_page.py`
3. Verify nav appears in output
