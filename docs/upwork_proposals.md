# Upwork Proposals — Ready to Paste

## 1. Senior AI Engineer and Architect (Healthcare, LLMs, Claude Code) — Bid: $85/hr

```
I've spent the last year building exactly what you're describing — a multi-agent AI platform with Claude as the primary model, agentic workflows with deterministic validation, and constitutional governance constraints that prevent hallucinations and silent failures in production.

My current system: 8-node distributed federation running Claude Code daily, 1,000+ autonomous engineering tasks completed with structured execution traces, 95K-memory semantic knowledge graph with topology-aware retrieval (83ms), democratic 7-specialist council that enforces quality gates on every consequential AI output. Python/FastAPI throughout, PostgreSQL with pgvector for semantic search.

Healthcare-adjacent: I built VetAssist (vetassist.ganuda.us), a disability calculator for veterans that handles sensitive medical data on sovereign infrastructure — no external API dependencies, data never leaves the cluster.

What maps directly to your needs:
- "Agentic architectures, evaluation loops, constrained generation" — my council topology does exactly this with concern flags, confidence scoring, and circuit breakers
- "Preventing hallucinations and silent failures" — I built corrective RAG with contradiction detection, drift monitoring, and automated safety canary testing
- "Claude Code for AI-assisted development" — I use it every day to manage the federation. 200+ structured instruction documents authored for AI agent execution
- "Observable, reproducible, constrained" — Fire Guard health monitoring every 2 minutes, Langfuse tracing, systemd timer orchestration

35 years of infrastructure including 8 years at Walmart managing ITSM platforms (6M transactions/day). Military veteran (E-7/SFC, 23 years). I build systems that run at 3 AM when nobody's watching.

Portfolio: ganuda.us/blog
```

## 2. Senior DevSecOps / Security Engineer — Bid: $95/hr

```
8 years on Walmart's Security Incident Management team — Red Team, Blue Team, SOC, and government-accredited forensics lab. I built the infrastructure these teams operated on, not just used it.

Current security stack: FreeIPA/Kerberos identity management across 6 Linux nodes, WireGuard encrypted mesh VPN, nftables per-node firewall policies, VLAN segmentation for PII isolation, credential scanning with gitleaks pre-commit hooks, automated safety canary testing daily at 3 AM, scoped NOPASSWD sudo rules with audit trail via SSSD.

Specific experience:
- Built air-gapped forensic processing labs with GPS-sourced NTP (no network connectivity for time sync)
- Deployed SureView surveillance infrastructure internationally over VPN under active threat conditions
- Designed sovereign AI inference that operates without external API dependencies
- AES-256 Fernet encryption for secrets management with automated rotation
- Currently operating an 8-node federation with keepalived VRRP failover on dual-homed DMZ nodes

I treat security infrastructure as context engineering — every access decision considers who is asking, through which path, with what authorization chain, in what environment state. Not static RBAC labels.

35 years enterprise infrastructure, 23 years military (E-7/SFC). I've built systems that passed government accreditation audits. Happy to discuss specifics on a call.
```

## 3. AI Engineer — Open-Source LLM Workflow Pipeline — Bid: $75/hr

```
I'm currently running an open-source LLM pipeline in production: vLLM serving Qwen2.5-72B-AWQ on an RTX PRO 6000 (32 tok/s), Ollama on Mac Studios for cross-platform inference, MLX for Apple Silicon deployment. No cloud dependencies, no API costs, fully sovereign.

The pipeline includes:
- Multi-model orchestration: 7-specialist council queries the LLM with different system prompts in parallel, synthesizes responses
- Semantic memory retrieval: 95K+ memories with pgvector embeddings (BGE-large 1024d), HyDE query expansion, corrective RAG
- Topology-aware graph retrieval: 385K-edge knowledge graph with 83ms path search (S-PATH-RAG implementation)
- Autonomous task execution: 1,000+ engineering tasks completed by AI agents through structured instructions
- Self-improving skill library: 687 skills auto-distilled from execution traces with UCB1 bandit selection

All Python, all open-source tooling, all running on consumer hardware. PostgreSQL backbone, systemd service orchestration, WireGuard networking.

If you need someone who has built and operated an open-source LLM workflow pipeline end-to-end — not just prototyped, but run in production for months — that's what I do every day.

Portfolio: ganuda.us/blog
```

## 4. Senior Python / AI Engineer (LLM + Decision Systems) — Bid: $65/hr

```
I built an LLM-powered decision system that's been running in production for months: a 7-specialist democratic council that evaluates proposals through parallel LLM queries, scores confidence, detects drift, and enforces constitutional constraints before any consequential action is taken. 8,975 recorded votes with full audit trail.

The decision pipeline:
- Each specialist queries the LLM with a domain-specific system prompt (security, strategy, market, legal, etc.)
- Responses scored for reasoning depth — shallow responses get re-prompted
- Adversarial member (Coyote) structurally required to dissent, preventing groupthink
- Vote similarity scoring detects when agents converge suspiciously vs genuinely agree
- Four-signal reward function: validity, continuity, grounding, drift detection

Python throughout. PostgreSQL, FastAPI, pgvector semantic search, vLLM inference. 35 years of production infrastructure experience, currently building full-time on AI decision systems.
```

## 5. Enterprise Integration — APIs / ServiceNow / Identity — Bid: $55/hr

```
11 years on the BMC Remedy ITSM team at Walmart — the enterprise ticketing/change management/incident tracking platform serving 6M transactions/day across 12 web servers, 6 app servers, and 1TB RAM database nodes. Led the migration from HP-UX to SUSE, Informix to Oracle (completed in 4 hours on an 8-hour window), and later to AIX.

Identity management: Currently running FreeIPA with Kerberos authentication across a 6-node Linux cluster, SSSD sudo integration with scoped rules and audit trails. Previously managed enterprise identity for 5,000+ Unix/Linux systems.

API integration: Built RESTful services with FastAPI, Gmail API integration, Telegram bot API, WebSocket real-time updates. Currently operating a multi-service architecture with Caddy reverse proxy, keepalived failover, and F5 load balancing experience from Walmart.

I understand enterprise integration at the infrastructure level — not just connecting APIs, but making them reliable, observable, and maintainable at scale.
```

## 6. Claude AI Implementation — Law Firm — Bid: $65/hr

```
I use Claude Code professionally every day to manage an 8-node AI federation. I've authored 200+ structured instruction documents for AI agent execution, built prompt governance through a democratic council system, and implemented quality gates that prevent AI outputs from shipping without validation.

For a law firm context, I can design:
- Structured prompt libraries with version control and governance (your exact ask)
- Validation workflows ensuring AI-generated content is checked against source documents before use
- Context engineering pipelines that feed relevant precedents, case law, and firm standards into Claude's context window
- Audit trails for every AI-assisted output (who prompted, what was generated, who approved)

I built my governance system specifically because ungoverned AI outputs are unreliable — the same principle that makes AI in legal work risky is what I've been solving architecturally. Every AI output in my system passes through quality gates before it's trusted.

35 years enterprise infrastructure, comfortable with SharePoint and document management systems from the Walmart corporate environment.
```

## 7. AWS Cloud Architect — Secure AI Infrastructure (8 weeks) — Bid: $75/hr

```
Currently architecting and operating secure AI infrastructure across an 8-node federation: vLLM inference on NVIDIA RTX PRO 6000, PostgreSQL with pgvector semantic search (95K+ records), WireGuard encrypted mesh VPN, FreeIPA/Kerberos identity, nftables firewall hardening, keepalived VRRP failover on dual-homed DMZ nodes.

While my current stack is on-premises (sovereign by design), the architectural patterns translate directly to AWS: VPC segmentation = my VLAN isolation, IAM = my FreeIPA RBAC, ECS/EKS = my systemd service orchestration, RDS = my PostgreSQL backbone, Bedrock = my vLLM inference layer.

For a secure AI infrastructure project, I bring:
- Air-gapped architecture design experience (built forensic labs with no network connectivity)
- Database administration (PostgreSQL, Oracle DBA backup/recovery/performance tuning)
- Security-first mindset from 8 years supporting Red Team/Blue Team/SOC at Walmart
- 23 years military (E-7/SFC) — security clearance culture is native to me

Happy to scope the 8-week engagement on a call.
```
