version: "3.8"

services:
  sag-v2:
    build: .
    ports:
      - "${SAG_V2_PORT:-4100}:4100"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4100/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3